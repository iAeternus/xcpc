#include <bits/stdc++.h>

using usize = unsigned long long;

using u8 = unsigned char;
using i8 = char;

int main() {
    for(usize i = 10; i >= 0; --i) {
        std::cout << i << std::endl;
    }

    const i8* str = "aaabbb";
}