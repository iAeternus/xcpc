#include <bits/stdc++.h>
#include <algorithm>
#include <map>
#include <vector>
#include <cmath>

typedef long long i64;
#define PI 3.1415926535

int main() {
    // double r = sqrt(666.0 * 666.0 + 233.0 * 233.0);
    // double l = atan2(666.0, 233.0) * r;
    // std::cout << (int) (r + l) << std::endl;
    std::cout << 1576 << std::endl;
    // //	std::cout << 666 + 233 + 666 * PI / 2;
    // //	int i = 2025 * 2025;
    // //	std::cout << i;
    // 	std::cout << 2025 * 2025 * 2025 * log2(2025);
    return 0;
}
